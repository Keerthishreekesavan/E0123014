{
  "name": "logging-middleware",
  "version": "1.0.0",
  "description": "Reusable logging middleware that validates input, attaches Bearer Token, and posts structured logs to the evaluation service",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon src/index.js"
  },
  "keywords": ["logging", "middleware", "express", "nodejs"],
  "author": "Keerthishree K (e0123014)",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.6.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
